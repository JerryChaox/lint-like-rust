# 围绕真实语料重新编排

2026-09-13 审计：测试从 204 增至 240，固定 Museon 验收仍为 2/11。已有 CFG、异常后继和状态传递是真实实现，但连续按单一机制结束任务，使可运行功能没有汇合成用户可验收的结果。责任在任务拆分与结束条件，不是用户授权或工具不可用。对应 LoopX `tiny_turn_under_delivery` 模式的实现型变体。

## 当前权威证据

- 固定目标与来源：`tests/corpus_v2/manifest.json`，3 份源摘录、11 变体，哈希校验通过。
- 实际结果：`reports/v2/corpus.json`、`reports/v2/corpus.md`，2/11。
- 模型范围：`docs/v2/SEMANTICS.md`；不得把未知转成成功。
- `read_file` 原始/修复/跨函数修复均有 4 个 verified、3 个 unverified；两个错误变体保持 LIFE001。
- 三个共同缺口：Exception type matching/binding；Implicit exception coverage；context-manager implicit exception edges。

## 下一批结束条件

保持五个 read_file 变体的源码、哈希、预期与检查范围不变；原始/两种修复达到既有目标，两种错误继续被检出。全局目标因此至少为 5/11，仍不代表整体目标完成。不得以新增测试数量、一个 IR 变体或一个语法节点实现完毕作为下一批交付结束条件。若确有未解问题，记录能复现的语义反例和依赖，继续解决，而非把前置步骤包装成验收完成。

## 同一批中的依赖顺序

1. 异常事实域：正常/异常退出携带异常类别集合及 unknown；匹配返回确定匹配、不匹配或未知，维护内建符号身份与遮蔽。普通 Exception 与 BaseException 子类不能混同。类别未知继续有显式义务。
2. 将 acquire/read/write/close 的类别与前后资源效果接入异常后继；跨函数保留类别和状态关联，不能先无条件合并再声称精确匹配。打开失败无新资源；关闭失败保留适当状态。
3. 清理语义：标准文件 with 的正常退出、return、显式/隐式异常、嵌套清理；清理失败覆盖原退出原因，并继续外层清理。未知 context manager 的抑制与自定义方法保持未知。
4. 覆盖证明：将无条件的 try/with Unknown 替换为由子操作和清理义务推导的覆盖状态。只有所有相关效果均建模才消除对应 gap；绝不按函数名或语料路径特殊处理。
5. 一次完成异常域、前端、求解器、诊断覆盖和固定案例回归；执行完整 cargo 检查、来源验证、五变体与整个语料评估。修复变未知必须继续被 compare 拒绝。

## 反例约束

保留未知子类、被遮蔽的 Exception/open、未知回调、动态类型元组、异常别名删除、read/close 失败、关闭后读取、嵌套 with 清理失败、return 被清理异常替换、未知异常匹配、捕获与未捕获跨函数异常。仅正常路径检查不能替代既有异常缺口验收。

本次是执行计划修复，不声称新增产品能力；不消费交付 quota slot。下一批仍由 llr-rewrite 本地实现，Museon 只读，不发布或提交外部内容。
