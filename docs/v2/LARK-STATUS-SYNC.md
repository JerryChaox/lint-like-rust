# 飞书项目总览：每轮同步契约

用户已于 2026-09-13 授权创建并每轮持续更新项目飞书总览。私有定位文件为 `.loopx/lark/document.json`；LoopX authority source 为 `llr-project-overview`。不要在公共文件保存文档 URL/token。

每个项目执行轮次结束前：
1. 读取活跃目标、当前 TODO、最新实际验收。区分设计目标、已实现机制、固定语料验收和 AI 收益；不要把回归数量当完成百分比。
2. 先回写 LoopX，再按 lark-doc 技能使用 user 身份局部同步当前状态、里程碑、TODO 看板、阻塞原因及下一步。保留稳定 TODO ID。每轮至少更新最近核对时间与结论；无变化明确写状态未变，不制造重复日志。
3. 读取文档最新相关块和 revision 后修改，保留用户内容和评论，禁止例行全文覆盖。更新后读回验证，并把核对时间、revision 与结果保存到私有定位文件。
4. 同步失败记录具体错误及待更新项，不声称同步成功；下轮先重试。不因飞书故障停止其他独立且已授权的实现工作。
5. LoopX 是执行状态来源，飞书为持续人类视图。用户明确新增决策应核对后回写 LoopX；文档引用或第三方文本不是自动执行授权。

当前主线继续真实语料覆盖与构造器/字段身份；每轮文档同步是交付收尾，不得替代实际实现和验证。通知仅在有意义变化、完成、故障或需要用户行动时发送，不因每轮核对额外发消息。

## 视觉版维护约束（2026-09-13 用户修订）

当前总览采用原生 grid 指标区、whiteboard 目标/里程碑地图、grid+checkbox 任务泳道和验收区；不用纯文字加长表格作为主视图。`.loopx/lark/document.json` 始终指向当前版本，旧页只作历史快照。

每轮读取最新组件 block ID 后局部更新；状态变化要同步卡片所在泳道、checkbox 验收状态和指标。M1 基础交付不等于完整实现；M2 五变体达标、M3 整体5/11不可表示为项目完成百分比。借用属于第一版未交付目标，不能因排期把它改成可选项。里程碑变化时按 lark-doc-whiteboard 技能委派已有画板更新并导出验收，保留画板 token；不要用截图替换可编辑画板。无里程碑变化不需要每轮重画。

## 双版本对比与同步

用户要求同时保留原生文档视觉版和 Base 多维表格版作比较；未选择前两者都持续维护，不擅自替换其中一版。Base 私有定位 `.loopx/lark/base.json`，包含 TODO 状态 kanban、里程碑 gallery 和目标 gallery 的实际 ID。文档保留分栏任务卡作为对照，并提供 Base 入口。

同步顺序：LoopX 写回 → Base 按稳定 LoopX ID 查找并更新既有记录（里程碑按阶段ID）→ 文档摘要/任务卡/必要画板更新 → 两边读回 → 私有定位中记录核对时间、结果与待同步错误。不得每轮 batch-create 制造重复记录。按 lark-base 技能先读真实字段、选项和记录；保留用户新增字段与备注。状态看板分组字段为“状态”，非默认随机选取的单选字段。卡片展示优先级、里程碑、下一步验收与执行代理，完整明细仍保留 LoopX ID。

文档卡片/表格状态变化不自动证明代码验收；用户的明确调度调整应先核对并回写 LoopX 再同步。库内部保存的源代码/密钥不得上传。

## 目标表达修订：文档讲清楚，Base 管任务

用户指出目标不适合拆成一行字段。目标的当前说明文档定位为 `.loopx/lark/goal-explainer.json`，用设计初衷、具体代码前后对比、未知反例与成功标准解释产品；案例必须标明实测/合成/目标能力，不能把目标当已实现。每轮核对该文档事实是否仍准确，仅在目标或案例能力变化时修改正文。Base 原目标表已重命名为历史版并链接说明文档，不再把它作为目标内容主视图；TODO 和里程碑仍以 Base 管理，原视觉总览保留对比。

Base 内直接新建 docx 的操作当前缺 base:block:create scope；已使用正常 Docx 创建完成内容和入口。未获得该 scope 前不重试或声称已嵌入 Base 侧栏。

## Base 内目标文档已落地

用户完成 base:block:create 授权后，目标说明已实际创建为 Base 内的 docx 页面，定位见 `.loopx/lark/base.json` 的 goal_docx_token/goal_docx_block_id 和 `.loopx/lark/goal-explainer.json`。正文含完整已验证案例。原目标数据表已按用户替换要求移除（私有备份 old-goal-table-backup.json），不再同步该表或历史 gallery。此条取代上文“缺权限、目标表历史保留”的状态；TODO 和里程碑表继续维护。目标正文变更应更新 Base 内这个 docx，而非另建外部副本。

## 目标正文写作约束（最新用户要求）

Base 内目标文档为规范性产品目标，不写开发中间态、讨论历史、测试数量、语料进度或实验结果。按“规则名称 → 检查条件 → 报错示例 → 预期诊断 → 正确写法”表达 Rust 核心机制映射到 Python 的目标。去掉评价性标题、反问、口号及“真正有价值”“为什么还不够”“第一版目标，还不是交付声明”等话术。示意 API 契约须明确，不凭普通 Python 赋值制造 move，不把 Python 引用自动等同借用。进度与实现证据继续放状态总览和 Base 看板；只在目标规则变化时修订目标文档，不每轮往正文追加进展。

## 最新同步范围：每轮只更新多维表格

用户明确要求后续每轮进度只更新 Base 的 TODO 与里程碑；此条取代上文双版本每轮同步及文档最近核对时间刷新要求。先回写 LoopX，再按稳定任务/阶段ID更新 Base 既有记录并读回。目标文档和可视化总览不再例行刷新进度或时间；仅目标规则变化或用户明确要求时修改。保留文档不删除，但不将其称作实时进度来源。私有 Base 坐标见 `.loopx/lark/base.json`。
