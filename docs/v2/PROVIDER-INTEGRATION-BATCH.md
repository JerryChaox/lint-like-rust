# 实时提供者集成批次

触发：LoopX no_progress_streak；最近三轮增加证据入口与声明映射，固定语料仍5/11。归属为任务拆分过细与旧Todo标题漂移，不是环境故障。保留已验证代码，不再按格式层拆交付。

下一批次必须同时完成：

1. 复用固定版本受控TSP探针，查询跨模块函数、同名不同声明、混合/未知目标。
2. 在探针临时源码仍存在时，将归一化事实及目标文件哈希传给已安装llr，验证真实协议range与AST匹配。
3. 证明过期源码/目标被拒绝，动态替换和混合候选仍Unknown；所有执行探针保持未触发。
4. 对同一函数链报告无证据/附证据的诊断差异及证明范围，不把附加元数据计成安全覆盖提升。
5. 重跑固定11项并记录未改善的原因。若实时候选仍不能安全缩小调用目标，停止扩充展示层，转向已有确定调用图的资源/所有权语义目标；不要为名义类型发明确定性。

验收命令：扩展 scripts/probe_type_server.py 的CLI集成验收后运行；cargo test、fmt、clippy；固定语料与哈希校验。确切探针参数从现有 --help/依赖定位读取。唯一外部进程为受控分析器与本地llr，不导入或执行Museon。
