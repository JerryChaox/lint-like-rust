# Museon `llr lint` 测量与人工复核

测量日期：2026-09-13。使用 release 构建对 Museon 仓库根目录运行 `llr lint --format json`；扫描只读取 Python 源文件，没有执行、导入或修改目标代码。

## 结果

- 扫描文件：4580 个 Python 文件
- 耗时：33.60 秒（目标 ≤120 秒，达标）
- 最终发现：0

没有可供逐条判定的发现。原因是现有已建模规则在全仓运行后只产生了三条 `Possible` 候选；人工查看上下文发现它们均位于正常的 `with open(...)` 作用域内，而且其证据来自控制流汇合后的事实集合。V2 求解器不保证这种集合构成一条有序可执行路径，因此这些候选不满足本 spike 的“直接证据链”要求，最终 lint 不输出它们。未为增加发现数而放宽证据门槛。

## 汇总

- 发现数：0
- TP 数：0
- FP 数：0（最终输出）
- Precision：N/A（0/0，无发现，不能定义 precision）

本次测量说明：当前已有规则在 Museon 全仓的高精度 best-effort 模式下没有找到可报告的真实问题；后续规则选择应由 N1 的真实修复分类决定，而不是由本次零发现测量反推。
