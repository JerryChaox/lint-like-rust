# 字段组合与哈希入口范围核验

revision19 未变。本轮没有新增模型假设，也没有修改固定语料。

## 字段组合

tests/python_fields_v2.rs 新增跨模块嵌套字段、字段类型冲突、描述符/继承/动态属性反例、跨调用返回字段别名。均通过；字段测试共11项，其中最后一项验证哈希入口范围。不同字段上下文类型冲突保留 Unknown，属性协议不明确时不会套用标准文件操作。

## 哈希缺口的可复现实验

scripts/probe_hash_entry_scope.py 读取固定 original/mutated_error/repaired 原文字节，仅在临时目录追加 run 调用 _sha256(Path('x'))，静态分析 run。三个退出码分别0/1/0。原函数入口的5/6/5项未知由没有证明精确Path来源开始，向open、上下文管理器、read、sentinel和digest.update传播。循环机制在精确调用上下文中已工作。

结果 reports/v2/hash-entry-scope.json 保存原文哈希、入口、范围及完整诊断。新增调用者是合成开发上下文，不是Museon原文入口；不计入固定语料5/11，不声称真实项目调用链已经证明。

## 真实调用链仍需证明的事实

font_catalog.py 的 FontCatalog.__init__ 在 self._root、环境字符串、可选root、manifest下标及路径运算后调用_sha256。新字段身份只覆盖其中的对象保存和传递；环境结果、JSON容器、分支接收者、下标效果仍缺事实。media_io.py 另有同名函数，调用前经过下载/存储与未知方法效果，不能仅凭名字借用上述证明。

_append_brain_log 的未知来自Path(state_root)输入协议、格式化、路径运算、mkdir、json.dumps(record)及字符串拼接。dict标注并不能证明任意嵌套值及子类回调无效果，str标注也不应直接等同精确内置str。

## 下一批次

停止为原哈希循环继续增加无关语法。优先把现有结构化类型提供者适配器接入静态前端：快照/声明位置映射、受控执行边界、nominal与exact区分、未知效果保留。以真实调用上下文中哪些事实可证明为验收；不要要求名义类型提供者证明运行时精确性。固定11项继续单独报告，合成上下文实验单独报告。所有权/借用仍为未完成目标。
