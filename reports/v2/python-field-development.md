# Python 直接字段开发验收（revision19）

Python AST 前端将封闭普通类的字段读写连接到 revision18 堆图。构造器保存资源后，跨方法访问和实例别名共享同一资源身份。字段类型按类和字段名迭代推导；冲突和未解析值扩大为 Unknown。读取生成临时值，保留读取当时的引用；字段赋值在可能失败的分支之后更新堆。

新增 tests/python_fields_v2.rs 六项回归覆盖构造器/方法、不同实例、字段重绑定与旧引用、未知写入/方法覆盖、异常写入、分支缺失字段。全量311测试、fmt、clippy通过，release和本地CLI已安装。

examples/v2/field-flow 三个静态开发案例：关闭字段文件后读取退出1；调整读取顺序退出0；未知外部修改退出3。比较分别为 resolved 和 became_unverified。报告存于 reports/v2/field-flow/。不执行案例。

固定Museon语料仍5/11，3原始片段及11变体哈希校验通过。此批次未达到剩余固定语料验收，不宣称借用或所有权完成。

下一步：补跨模块和嵌套字段、类型冲突及描述符边界的组合验收；将字段/调用事实用于剩余真实语料的具体缺口分析，再确定整组修复。继承、动态属性钩子、描述符与未知调用保持未知。
